---

circleCi:
  apiToken: "not-really-a-token"
gitlab:
  sharedSecretForHooks: "super-secret"
statsd:
  host: "localhost"
  port: 123
  refreshPeriodSeconds: 3
domainMapping:
  repositories:
    123: gh/ghorg/ghrepo